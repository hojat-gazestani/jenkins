

pipeline {
    agent {
        label "kubeagent"
    }
    stages {
        stage('Git clone') {
            steps {
                container('jnlp'){
                    git branch: 'main', url: "https://github.com/hojat-gazestani/myapp.git"
                }
                
            }
        }
    }
}